# 작업 회고

## 기술 선택

- 토이 프로젝트라 하여 정말 장난감처럼 가지고 놀고싶다는 생각에 평소 관심있던 [`Effect`](https://effect.website/)를 적용해보았다.
- 기왕 개인 블로그를 Cloudflare에 배포하고 있는 김에 이것도 여기다 배포해버리면 좋겠다는 생각이 들어 Cloudflare Workers를 선택했다.
  - Pages였어도 문제는 없었겠는데 둘이 결국 합쳐진다는 이야기를 들어서 적응도 할 겸...
- 이미 블로그에 적용 중이긴 하지만, [`PandaCSS`](https://panda-css.com/)를 선택했다
  - Tailwind에 절어버린 뇌에 생명의 숨결을
- React Element로 이미지를 그린다는 그 생각이 신박해서 [`Satori`](https://github.com/vercel/satori)를 선택했다.
- 최-신 개발 환경을 마음껏 즐기기 위해 에디터로 [`Zed`](https://zed.dev/), JS 툴체인으로 [`oxc`](https://oxc.rs/)를 선택했다.

## 좋았던 점

- 타입 안전한 DI가 쓰는 느낌이 좋았다
  - ex. `FontService`를 가져다 쓸 때 실제 구현체인 `FontServiceLive`를 provide해줘야 에러가 없어짐
- (삽질하면서 거의 사라졌지만) 어떠한 액션이 실패하거나 값이 없는 경우를 명시적으로 다루는 문법이 참 마음에 들었다(`Some<T>`로 받아서 `None`이면 로그만 찍고 filter out해버린다던가)
- 모든 것은 프로그램이 어떻게 돌지에 대한 정의이고, 실제 실행은 `runPromise`를 호출했을 때야 비로소 이루어지는... 구현부와 실행부가 완전히 분리된 느낌!

## 힘들거나 아쉬웠던 점

- Worker라는 환경에 대한 이해(결국 적응 못하고 Client Side Rendering으로 도망쳐옴)
- 글꼴 로딩이라던지 이미지 생성을 서버 사이드에서 하고 싶었는데 여러 제약 및 스킬 이슈로 적용하지 못함...
  - 글꼴 로딩까지는 성공했는데 서버 사이드에서 Satori를 쓰기 위해 wasm을 초기화하는 과정을 넘지 못했다...
  - 일단 파악한 표면 문제 상황은, wasm 파일을 로드하는 데 시간이 꽤(컴퓨터 기준) 걸리는데 해당 과정이 Free Tier인 내 계정은 Cloudflare가 허용한 바운더리를 벗어날 정도로 오래 걸리는 작업이었음
  - 아마 돈을 좀 더 내면 될 것 같긴 한데 시간이 넘 촉박하여...
- 대부분의 작업 시간을 이거 돌아가게 하려고 썼는데 결국 해결을 못해서 너무 힘들고 아쉬웠음
  - 추가로 넣을 수 있는 기능들이 있었을텐데(글꼴 선택? 제목/소제목 위치 설정 등)

## 이후로 시간이 난다면 넣고 싶은 기능

- 글꼴 선택!!!
- 임의 이미지 입력받아서 Base64 Data URL 변환 후 사용
- 이미지 변환 이외 작업(Form Validation 등)
